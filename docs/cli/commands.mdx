Coming eventually...

- `cozy run`: runs the cozy-creator server
- `cozy download`: downloads models from Hugging Face Hub
- `cozy build`: discovers cozy-plugins installed in the Python environment, builds the Golang server binary, and transpiles + bundles + minifies the frontend

